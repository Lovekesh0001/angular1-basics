import { FormsModule } form '@angular/forms';     == import this in app.module.ts file


<input type="text" [(ngModel)] = "name">   == banana in the box 
{{ name }}                                 == when type in input this will give the value as well